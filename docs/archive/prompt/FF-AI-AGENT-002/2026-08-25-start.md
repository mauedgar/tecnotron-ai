# Prompt de inicio — continuación FF-AI-AGENT-002

> **Precedencia 7 (no canónico).** Este archivo vive en `docs/archive/`; es una
> brújula de navegación, no source of truth ni autoridad. La evidencia viva del
> trabajo reside en `docs/tasks/FF-AI-AGENT-002/`. Ante cualquier discrepancia
> ganan: AGENTS.md → `docs/SOURCE_OF_TRUTH.md` → TASK canónica → Developer.
>
> **Estado al cierre de la sesión origen: 2026-08-25.** Si `origin/tooling`
> avanzó desde entonces, re-verificar todo con los comandos de abajo.

## Contexto mínimo (verificado al cierre)

| Ref | SHA | Significado |
|---|---|---|
| `origin/tooling` base | `bc4b98d931fc372cfe6861ecc7f8938a1b5eaa9b` | Merge PR #14 (cierre DOC_SYNC FF-AI-AGENT-001) |
| PR #12 | merge `3d5d8b85a316233eae029963a3f5d14400fcd7fc` | FF-AI-AGENT-001 integrada |
| PR #13 | merge `1cc64d46621c21ed1c8a8d877772facd2afb01de` | deps `.opencode` plugin 1.18.22 |
| Rama de trabajo | `feat/agent-profile-conformance` | Commit de preservación `cdd4c6474c2bbe8bb6ff4459d4ea428b4c305452` sobre `bc4b98d` |

## Estado de FF-AI-AGENT-002

- Docs-only: 3 archivos nuevos + 5 modificados (los 8 ownership keys en TASK).
- `validation: PASS` · review independiente: `ACCEPT_WITH_NON_BLOCKING_FINDINGS`
  (AC1–AC12 PASS; findings restantes LOW/INFO no bloqueantes).
- `developer_acceptance: PENDING` · `lifecycle_status: PENDING_ACCEPTANCE`.
- El draft `docs/architecture/agent-profile-conformance.md` NO está promovido a
  `canonical`; la task NO está integrada ni `DONE`.

## Pasos para el chat nuevo

1. Crear worktree de task desde el tooling vigente y checkout de la rama:
   ```bash
   git fetch origin
   git worktree add <ruta> feat/agent-profile-conformance
   ```
2. Re-ejecutar validación determinista (no heredarla del prompt):
   ```bash
   git diff --check origin/tooling..HEAD
   git diff --name-only origin/tooling..HEAD   # exactamente 5 modificados
   git diff --stat origin/tooling..HEAD        # ~321 inserciones, scope docs-only
   ```
3. Presentar el diff a la Developer para aceptación explícita.
4. Con `ACCEPTED`: promover `status: draft → canonical`, fijar
   `developer_acceptance: ACCEPTED` + `accepted_at`, abrir PR contra `tooling`,
   integrar (registrar `integration.sha`) y ejecutar `DOC_SYNC`.
5. Solo con las cinco dimensiones satisfechas y ruling del Developer:
   `lifecycle_status: DONE`.

## Prohibiciones vigentes

- Sin OpenCode/runtime/registries/adapters ejecutables ni perfiles; sin
  fallback, ranking ni model policy; sin leer/escribir FitFlow (`roles.yaml`
  permanece UNKNOWN).
- No instalar/actualizar dependencias sin decisión del Developer.
- No promover nada a `DONE` sin autoridad terminal del Developer.
- Paid API disabled.

## Notas de entorno

- `node_modules` de cada worktree es compartido/junction con
  `C:\Proyectos-Web\FitFlow-ai\node_modules`; nunca usar `rm -rf` recursivo
  sobre worktrees, solo `git worktree remove`.
- Worktrees y ramas ya mergeadas (PR #11–#14) fueron eliminados al cierre de la
  sesión origen; sus merges permanecen en el historial de `tooling`.
- `stash@{0}` global (rama VNEXT-007) quedó intacto por decisión propia; no
  tocarlo sin instrucción del Developer.
