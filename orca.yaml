scripts:
  setup: |
    set -e
    cd "$ORCA_WORKTREE_PATH"
    cp "$ORCA_ROOT_PATH/.env" "$ORCA_WORKTREE_PATH/.env" 2>/dev/null || true
    npm install

worktree:
  sharedDirectories:
    - node_modules